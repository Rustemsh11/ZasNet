# 🐳 ZasNet - Docker Setup

Полная настройка Docker Compose для запуска ZasNet (API + Web Client + PostgreSQL) в контейнерах.

## 📋 Что было создано

### ✅ Файлы конфигурации

- `docker-compose.yml` - главная конфигурация Docker Compose
- `.dockerignore` - исключения для Docker build
- `ZasNet.WebApi/Dockerfile` - уже существовал
- `ZasNetWebClient/Dockerfile` - создан для веб клиента
- `ZasNetWebClient/nginx.conf` - конфигурация nginx для Blazor WASM

### ✅ PowerShell скрипты

- `start.ps1` - запуск всех сервисов
- `stop.ps1` - остановка сервисов
- `logs.ps1` - просмотр логов
- `status.ps1` - проверка статуса

### ✅ Документация

- `QUICK_START.md` - быстрый старт (начните с этого!)
- `DOCKER_README.md` - подробная документация
- `DOCKER_SETUP_SUMMARY.md` - техническая сводка

## 🚀 Быстрый старт

### 1. Убедитесь что Docker Desktop запущен

### 2. Откройте PowerShell в директории проекта

```powershell
cd C:\Users\rsh\source\repos\ZasNet
```

### 3. Запустите скрипт

```powershell
.\start.ps1
```

### 4. Откройте браузер

- **Web Client**: http://localhost:8080
- **API**: http://localhost:5142
- **API Health**: http://localhost:5142/health

## 🎯 Основные команды

```powershell
# Запуск
.\start.ps1

# Остановка
.\stop.ps1

# Статус
.\status.ps1

# Логи всех сервисов
.\logs.ps1

# Логи конкретного сервиса
.\logs.ps1 -Service api
.\logs.ps1 -Service webclient
.\logs.ps1 -Service postgres
```

## 📦 Архитектура

```
┌─────────────────────────────────────────────┐
│            Docker Compose                    │
│                                              │
│  ┌──────────┐  ┌──────────┐  ┌───────────┐ │
│  │PostgreSQL│◄─┤   API    │◄─┤Web Client │ │
│  │  :5432   │  │  :5142   │  │   :8080   │ │
│  └──────────┘  └──────────┘  └───────────┘ │
└─────────────────────────────────────────────┘
```

## 🔧 Настройка (опционально)

Создайте файл `.env` для изменения настроек:

```env
POSTGRES_PASSWORD=ваш_надежный_пароль
JWT_SECRET_KEY=ваш_секретный_ключ_минимум_32_символа
TELEGRAM_BOT_TOKEN=ваш_токен_если_используете
TELEGRAM_CHANNEL_ID=ваш_id_канала
TELEGRAM_WEBHOOK_SECRET=ваш_webhook_секрет
```

## 📚 Дополнительная документация

- **[QUICK_START.md](QUICK_START.md)** - Быстрый старт с минимальными инструкциями
- **[DOCKER_README.md](DOCKER_README.md)** - Подробная документация по использованию
- **[DOCKER_SETUP_SUMMARY.md](DOCKER_SETUP_SUMMARY.md)** - Техническая сводка и архитектура

## ⚡ Полезные команды Docker

```powershell
# Просмотр статуса контейнеров
docker-compose ps

# Просмотр логов
docker-compose logs -f

# Перезапуск конкретного сервиса
docker-compose restart api

# Пересборка и перезапуск
docker-compose up -d --build api

# Остановка и удаление контейнеров
docker-compose down

# Остановка и удаление контейнеров + volumes (УДАЛИТ ДАННЫЕ!)
docker-compose down -v
```

## 🐛 Troubleshooting

### Порт уже используется

Измените порты в `docker-compose.yml`:

```yaml
ports:
  - "8080:80"  # Измените 8080 на другой порт
```

### Контейнеры не запускаются

```powershell
# Проверьте логи
docker-compose logs

# Проверьте Docker Desktop
# Убедитесь что он запущен и работает
```

### API не может подключиться к БД

```powershell
# Проверьте статус PostgreSQL
docker-compose ps postgres

# Проверьте логи PostgreSQL
docker-compose logs postgres
```

### Полная очистка и перезапуск

```powershell
# Остановить и удалить все (включая данные БД)
docker-compose down -v

# Запустить заново
.\start.ps1
```

## 🔐 Безопасность

⚠️ **Важно для продакшена:**

1. Измените все пароли и секретные ключи
2. Используйте файл `.env` для хранения секретов
3. Не коммитьте `.env` в Git
4. Используйте HTTPS в продакшене
5. Ограничьте доступ к портам

## 📊 Мониторинг

```powershell
# Статус и использование ресурсов
.\status.ps1

# Просмотр логов в реальном времени
.\logs.ps1

# Статистика Docker
docker stats
```

## 💾 Backup и Restore

### Backup базы данных

```powershell
docker-compose exec -T postgres pg_dump -U zasnet ZasNet > backup.sql
```

### Restore базы данных

```powershell
Get-Content backup.sql | docker-compose exec -T postgres psql -U zasnet -d ZasNet
```

## 🎉 Готово!

Теперь у вас есть полностью настроенная Docker среда для ZasNet.

Начните с **[QUICK_START.md](QUICK_START.md)** для быстрого старта!

---

**Создано:** 2 января 2026  
**Версия Docker Compose:** 3.8  
**Требуется:** Docker Desktop для Windows

