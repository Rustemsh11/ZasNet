# 📋 Краткая сводка - Docker Setup для ZasNet

## ✅ Что было сделано

Создан полный Docker Compose setup для запуска всей системы ZasNet в контейнерах.

## 🎯 Результат

Теперь вы можете запустить всю систему (PostgreSQL + API + Web Client) одной командой:

```powershell
.\start.ps1
```

## 📦 Компоненты

1. **PostgreSQL** - база данных (порт 5432)
2. **ZasNet API** - ASP.NET Core Web API (порт 5142)
3. **ZasNet Web Client** - Blazor WebAssembly (порт 8080)

## 📁 Созданные файлы

### В проекте ZasNet

#### Конфигурация Docker
- `docker-compose.yml` - главный файл конфигурации
- `.dockerignore` - исключения для Docker
- `.gitignore` - обновлен (добавлены Docker-related файлы)

#### PowerShell скрипты
- `start.ps1` - запуск всех сервисов
- `stop.ps1` - остановка сервисов
- `logs.ps1` - просмотр логов
- `status.ps1` - проверка статуса

#### Документация
- `START_HERE.md` - начните отсюда!
- `QUICK_START.md` - быстрый старт
- `README_DOCKER.ru.md` - главный README
- `DOCKER_README.md` - подробная документация
- `DOCKER_INDEX.md` - навигация по документации
- `DOCKER_SETUP_SUMMARY.md` - техническая сводка
- `DOCKER_CHECKLIST.md` - чеклист проверки
- `CHANGES_SUMMARY.md` - сводка изменений
- `SUMMARY_RU.md` - этот файл

### В проекте ZasNetWebClient

#### Docker файлы
- `Dockerfile` - конфигурация для сборки образа
- `nginx.conf` - конфигурация nginx для Blazor WASM
- `.dockerignore` - исключения для Docker

#### Конфигурация
- `wwwroot/appsettings.json` - настройки для development
- `wwwroot/appsettings.Production.json` - настройки для production

### Измененные файлы

1. **ZasNet.WebApi/Program.cs**
   - Добавлен health check endpoint: `/health`

2. **ZasNetWebClient/Program.cs**
   - Добавлено чтение API URL из конфигурации
   - Теперь можно менять API URL без пересборки

## 🚀 Как использовать

### Первый запуск

1. Откройте PowerShell в директории `C:\Users\rsh\source\repos\ZasNet`
2. Выполните: `.\start.ps1`
3. Откройте браузер: http://localhost:8080

### Ежедневное использование

```powershell
# Запуск
.\start.ps1

# Остановка
.\stop.ps1

# Просмотр логов
.\logs.ps1

# Проверка статуса
.\status.ps1
```

## 🌐 Доступ к приложениям

После запуска:

- **Web Client:** http://localhost:8080
- **API:** http://localhost:5142
- **API Health Check:** http://localhost:5142/health
- **PostgreSQL:** localhost:5432 (пользователь: zasnet, БД: ZasNet)

## 🔧 Настройка (опционально)

Если нужно изменить пароли или настройки, создайте файл `.env` в корне проекта ZasNet:

```env
POSTGRES_PASSWORD=ваш_пароль
JWT_SECRET_KEY=ваш_секретный_ключ
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_CHANNEL_ID=ваш_id
TELEGRAM_WEBHOOK_SECRET=ваш_секрет
```

## 📊 Архитектура

```
Docker Compose
├── PostgreSQL (порт 5432)
│   └── Volume: postgres-data (сохранение данных)
│
├── ZasNet API (порт 5142)
│   ├── Подключается к PostgreSQL
│   └── Health check: /health
│
└── ZasNet Web Client (порт 8080)
    ├── Nginx для раздачи статики
    └── Подключается к API
```

## ✨ Особенности

### Health Checks
- PostgreSQL: проверка готовности каждые 10 секунд
- API: HTTP запрос на /health каждые 30 секунд
- Автоматический перезапуск при падении

### Зависимости
- API ждет готовности PostgreSQL
- Web Client ждет запуска API

### Volumes
- Данные PostgreSQL сохраняются между перезапусками
- При `docker-compose down` данные НЕ удаляются
- Для удаления данных: `docker-compose down -v`

### Сеть
- Изолированная bridge сеть `zasnet-network`
- Контейнеры общаются по именам (postgres, api, webclient)

## 🛠️ Полезные команды

### Docker Compose

```powershell
# Просмотр статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f
docker-compose logs -f api

# Перезапуск сервиса
docker-compose restart api

# Пересборка и перезапуск
docker-compose up -d --build api

# Остановка
docker-compose stop

# Остановка и удаление контейнеров
docker-compose down

# Остановка и удаление контейнеров + данных
docker-compose down -v
```

### PostgreSQL

```powershell
# Подключение к БД
docker-compose exec postgres psql -U zasnet -d ZasNet

# Backup
docker-compose exec -T postgres pg_dump -U zasnet ZasNet > backup.sql

# Restore
Get-Content backup.sql | docker-compose exec -T postgres psql -U zasnet -d ZasNet
```

## 🐛 Troubleshooting

### Порт уже используется

Измените порт в `docker-compose.yml`:

```yaml
ports:
  - "8080:80"  # Измените 8080 на другой порт
```

### Контейнеры не запускаются

```powershell
# Проверьте логи
.\logs.ps1

# Проверьте статус
.\status.ps1

# Убедитесь что Docker Desktop запущен
```

### API не может подключиться к БД

```powershell
# Проверьте статус PostgreSQL
docker-compose ps postgres

# Проверьте логи PostgreSQL
.\logs.ps1 -Service postgres
```

### Полная очистка и перезапуск

```powershell
# Остановить и удалить все (включая данные)
docker-compose down -v

# Запустить заново
.\start.ps1
```

## 📚 Документация

### Начните с этого
1. **[START_HERE.md](START_HERE.md)** - самое краткое введение
2. **[QUICK_START.md](QUICK_START.md)** - быстрый старт

### Полная информация
3. **[DOCKER_INDEX.md](DOCKER_INDEX.md)** - навигация по всей документации
4. **[README_DOCKER.ru.md](README_DOCKER.ru.md)** - главный README
5. **[DOCKER_README.md](DOCKER_README.md)** - подробная документация

### Для проверки и отладки
6. **[DOCKER_CHECKLIST.md](DOCKER_CHECKLIST.md)** - чеклист проверки
7. **[DOCKER_SETUP_SUMMARY.md](DOCKER_SETUP_SUMMARY.md)** - техническая сводка

### Для разработчиков
8. **[CHANGES_SUMMARY.md](CHANGES_SUMMARY.md)** - что было изменено в коде

## 🔐 Безопасность

### ⚠️ Важно

1. **Не коммитьте .env файл в Git** - он уже добавлен в .gitignore
2. **Измените пароли** - не используйте пароли по умолчанию в продакшене
3. **Используйте HTTPS** - в продакшене настройте SSL
4. **Ограничьте доступ** - настройте firewall для портов

### Рекомендации для продакшена

1. Создайте .env файл с сильными паролями
2. Используйте случайный JWT секрет (минимум 32 символа)
3. Настройте HTTPS через nginx reverse proxy
4. Регулярно делайте backup БД
5. Обновляйте Docker образы

## 📈 Преимущества

### Для разработчиков
- ✅ Быстрый старт - одна команда
- ✅ Не нужно устанавливать PostgreSQL локально
- ✅ Одинаковое окружение у всех
- ✅ Легко очистить и начать заново

### Для DevOps
- ✅ Готово для CI/CD
- ✅ Health checks для мониторинга
- ✅ Автоматический перезапуск
- ✅ Легко масштабировать

## 🎉 Готово!

Теперь у вас есть полностью настроенная Docker среда для ZasNet!

### Следующие шаги

1. Прочитайте [QUICK_START.md](QUICK_START.md)
2. Запустите систему: `.\start.ps1`
3. Откройте http://localhost:8080
4. Изучите остальную документацию при необходимости

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи: `.\logs.ps1`
2. Проверьте статус: `.\status.ps1`
3. Читайте [DOCKER_README.md](DOCKER_README.md)
4. Используйте [DOCKER_CHECKLIST.md](DOCKER_CHECKLIST.md)

---

**Создано:** 2 января 2026  
**Версия:** 1.0  
**Статус:** ✅ Готово к использованию

**Начните с:** [START_HERE.md](START_HERE.md) или [QUICK_START.md](QUICK_START.md)

